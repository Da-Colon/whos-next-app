import { IListDetails } from "../../../context/ListContext/interfaces";

const ListsCardView = ({userLists}: {userLists: IListDetails[]}) => {
  return (
    <div></div>
  )
};

export default ListsCardView;
