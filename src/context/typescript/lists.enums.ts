export enum EListViewStates {
  Table,
  Card,
  // Web 3 (Full data + NFT?)
}

export enum EListFilters {
  Private,
  Public,
  None,
}

export enum ECreateListSteps {
  CreationMethod,
  NameAndSettings,
  Upload,
  ListCreation,
  Review,
}